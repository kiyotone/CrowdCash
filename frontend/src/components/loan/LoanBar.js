import React from 'react'
import { useSelector } from 'react-redux'

function LoanBar() {
  const user = useSelector((state)=> state.user)
  return (
    <div className='w-[40rem] h-30 text-red-600'>

        
        
    </div>
  )
}

export default LoanBar